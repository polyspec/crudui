# Rust bench build artifacts — regenerated by `cargo run --release`.
rust/target/

# Go build cache for the bench module (go run compiles into the shared cache,
# but keep any stray local binary out too).
go/polyspec-bench

# Last-run results are machine-dependent; regenerate with `make bench`.
results.md
