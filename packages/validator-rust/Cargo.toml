[package]
name = "crudui-validator"
version = "0.0.1"
edition = "2021"

[lib]
name = "crudui_validator"
path = "src/lib.rs"

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = { version = "1", features = ["preserve_order", "float_roundtrip"] }
