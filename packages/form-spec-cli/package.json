{
  "name": "@form-spec/cli",
  "version": "1.0.0",
  "private": true,
  "description": "form-spec orchestrator CLI — describe/check/validate/render/explain/scaffold/list-widgets. Thin wrapper that imports/parses the code single-source-of-truth (generator-core registry, validator-js rules, schema JSON, forbidden-scan, grammar docs). Holds no hand-copied catalog.",
  "type": "module",
  "bin": {
    "form-spec": "bin/form-spec.mjs"
  },
  "files": [
    "bin",
    "src"
  ],
  "scripts": {
    "describe": "node --import tsx bin/form-spec.mjs describe",
    "test": "vitest run"
  },
  "dependencies": {
    "@form-spec/generator-core": "file:../generator-core",
    "@form-spec/validator": "file:../validator-js"
  },
  "devDependencies": {
    "ajv": "^8.20.0",
    "ajv-formats": "^3.0.1",
    "js-yaml": "^4.2.0",
    "tsx": "^4.7.0",
    "vitest": "^4.1.8"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
