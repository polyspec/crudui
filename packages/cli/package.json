{
  "name": "@polyspec/cli",
  "version": "1.0.0",
  "private": true,
  "description": "polyspec orchestrator CLI — describe/check/validate/render/explain/scaffold/list-widgets. Thin wrapper that imports/parses the code single-source-of-truth (generator-core registry, validator-ts rules, schema JSON, forbidden-scan, grammar docs). Holds no hand-copied catalog.",
  "type": "module",
  "bin": {
    "polyspec": "bin/polyspec.mjs"
  },
  "files": [
    "bin",
    "src"
  ],
  "scripts": {
    "describe": "node --import tsx bin/polyspec.mjs describe",
    "test": "vitest run"
  },
  "dependencies": {
    "@polyspec/generator-core": "file:../generator-core",
    "@polyspec/validator": "file:../validator-ts"
  },
  "devDependencies": {
    "ajv": "^8.20.0",
    "ajv-formats": "^3.0.1",
    "js-yaml": "^5.2.1",
    "tsx": "^4.23.0",
    "vitest": "^4.1.9"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
