{
  "name": "@crudui/cli",
  "version": "0.0.1",
  "private": true,
  "description": "crudui orchestrator CLI \u2014 describe/check/validate/render/explain/scaffold/list-widgets. Thin wrapper that imports/parses the code single-source-of-truth (generator-core registry, validator-ts rules, schema JSON, forbidden-scan, grammar docs). Holds no hand-copied catalog.",
  "type": "module",
  "bin": {
    "crudui": "bin/crudui.mjs"
  },
  "files": [
    "bin",
    "src"
  ],
  "scripts": {
    "describe": "node --import tsx bin/crudui.mjs describe",
    "test": "vitest run"
  },
  "dependencies": {
    "@crudui/generator-core": "0.0.1",
    "@crudui/validator": "0.0.1"
  },
  "devDependencies": {
    "ajv": "^8.20.0",
    "ajv-formats": "^3.0.1",
    "js-yaml": "^4.2.0",
    "tsx": "^4.7.0",
    "vitest": "^4.1.8"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
