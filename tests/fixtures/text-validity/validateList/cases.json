[
  {
    "name": "pass-paired-surrogates",
    "note": "a surrogate pair is valid text in a column label.",
    "spec": {
      "columns": {
        "name": {
          "field": "name",
          "label": "A😀"
        }
      }
    },
    "expect": {
      "valid": true,
      "errors": []
    }
  },
  {
    "name": "spec-lone-high-in-label",
    "note": "a lone high surrogate in a column label is a load failure.",
    "spec": {
      "columns": {
        "name": {
          "field": "name",
          "label": "\ud800"
        }
      }
    },
    "expect": {
      "code": "INVALID_TEXT",
      "message": "Text must be Unicode scalar values",
      "at": "columns.name.label"
    }
  },
  {
    "name": "spec-member-name",
    "note": "an invalid column name is reported at the columns object.",
    "spec": {
      "columns": {
        "\udc00\ud800": {
          "field": "x"
        }
      }
    },
    "expect": {
      "code": "INVALID_TEXT",
      "message": "Text must be Unicode scalar values",
      "at": "columns"
    }
  },
  {
    "name": "spec-before-forbidden-key",
    "note": "text is checked before the forbidden-key scan.",
    "spec": {
      "columns": {
        "name": {
          "field": "name",
          "if": 1,
          "label": "\udc00"
        }
      }
    },
    "expect": {
      "code": "INVALID_TEXT",
      "message": "Text must be Unicode scalar values",
      "at": "columns.name.label"
    }
  },
  {
    "name": "file-document",
    "note": "invalid text in a file is located at the file name and path.",
    "spec": {
      "columns": {
        "name": {
          "field": "name",
          "label": "Name"
        }
      }
    },
    "files": {
      "cols.yml": {
        "properties": {
          "x": {
            "field": "\udc00"
          }
        }
      }
    },
    "expect": {
      "code": "INVALID_TEXT",
      "message": "Text must be Unicode scalar values",
      "at": "cols.yml.properties.x.field"
    }
  },
  {
    "name": "basepath",
    "note": "an invalid base path is an input failure naming the option.",
    "spec": {
      "columns": {
        "name": {
          "field": "name",
          "label": "Name"
        }
      }
    },
    "options": {
      "basepath": "\ud800"
    },
    "expect": {
      "code": "INVALID_FORM_INPUT",
      "message": "Text must be Unicode scalar values: options.basepath",
      "at": ""
    }
  }
]
