export const messages = {
  en: {
    title: 'Nested form verification', original: 'Original arrays + example controller', keyed: '13-character runtime',
    corrected: 'Original renderer with empty-collection correction',
    'original-keyed': 'Original keyed renderer + example binding', comparison: 'Original comparison',
    intro: 'Edit nested forms, submit to PHP, save JSON records and reload the stored hierarchy.',
    originalNote: 'The unchanged original library renders arrays. This diagnostic example adds a controller and hidden sequence fields. It does not meet the requirement to submit identity without additional fields.',
    originalKeyedNote: 'The unchanged original public functions compose and bind a cached structure. The example controller adds keyed row editing and saved-key updates. It uses the same 13-character data and PHP submission as the current runtime, without hidden sequence fields.',
    correctedNote: 'The original source has a separate correction for explicit empty collections and their Add buttons. The example uses the original public functions for cached binding and keyed editing. The unchanged original remains selectable.',
    invalid: 'Validation failed. Review the field errors.',
    keyedNote: 'The template is compiled once. The library session handles input, data injection and row operations. Native form names contain row identity. JSON processing must preserve object member order.',
    load: 'Reload saved data', blank: 'Mount empty form', save: 'Save to JSON', validate: 'Validate with PHP', reset: 'Reset saved example',
    saveNote: 'Saving replaces this frame’s complete JSON repository. Reset restores its initial records. Checks reset this repository before each case.',
    names: 'Native input names and values', state: 'Current form data', noState: 'No library session; values are read by the example controller.',
    php: 'PHP request, validation, storage and response', checks: 'Run the same checks', allChecks: 'Run all frameworks',
    results: 'Verification results', pending: 'Not run', pass: 'PASS', fail: 'FAIL', running: 'Running…',
    failureDetails: 'Assertion details',
    failureNotes: { original: {
      identity: 'Example configuration: the array example adds hidden sequence fields. The original renderer does not require them.',
      exact: 'Original renderer: an empty department collection creates a blank row, which native submission saves as a department. The empty check has the same cause.',
      empty: 'Original renderer: an explicitly empty collection still renders one row. The exact check has the same cause.',
      cache: 'Example binding: this array example calls buildForm for each update. It does not test the original public functions with separate cached composition.',
    }, 'original-keyed': {
      exact: 'Original renderer: an empty department collection produces an indexed placeholder. Native submission rejects its key. The empty check examines the same rendering behavior.',
      empty: 'Original renderer: an explicitly empty collection still renders one row. The example controller does not remove that row.',
    } },
    render: 'Nested names and values', plus: 'Add a company with nested fields', copy: 'Copy current values and edit descendants independently',
    identity: 'Submit identity without hidden sequence fields',
    keyedNames: 'Render keyed data and submit native names without hidden fields',
    nonsequential: '5 → 7 → 1: insert, copy, reorder and save',
    transport: 'Preserve order through the supported submission format',
    order: 'Reorder copied companies', inject: 'Reload after editing', saved: 'Copy, save new IDs and reload parent relationships',
    phpValid: 'PHP validates the native form', phpInvalid: 'Invalid save preserves stored records', cache: 'Reuse a serialized data-independent template',
    exact: 'Preserve the complete record through native submission and reload', empty: 'Empty collection display, addition, deletion and persistence',
    deletion: 'Persist reordered rows and delete descendants', shape: 'Reject incorrect collection types and child ownership',
    manual: 'Compare identical keyed data using the corrected original renderer and current runtime. The unchanged original keyed renderer remains selectable. Use the row buttons or load the 5 → 7 → 1 example. The earlier hidden-field array diagnostic remains selectable; it is not an accepted replacement.',
    source: 'Source commits and archive hashes', plusLabel: '+ Add', copyLabel: 'Copy', minusLabel: '− Remove',
    upLabel: '↑', downLabel: '↓', count: 'Company rows', download: 'Download results', framework: 'Framework',
  },
  ko: {
    title: '중첩 폼 검증', original: '원본 배열 + 예제 컨트롤러', keyed: '13자리 런타임',
    corrected: '빈 컬렉션을 수정한 원본 렌더러',
    'original-keyed': '원본 키 렌더러 + 예제 바인딩', comparison: '원본 비교 대상',
    intro: '중첩 폼을 편집하고 PHP에 제출한 후 JSON 레코드로 저장하고 저장된 계층을 다시 불러옵니다.',
    originalNote: '원본 라이브러리가 배열을 렌더링합니다. 이 진단 예제에는 컨트롤러와 숨김 seq 필드를 추가했습니다. 별도 필드 없이 식별자를 제출해야 한다는 요구사항을 충족하지 않습니다.',
    originalKeyedNote: '수정하지 않은 원본 공개 함수로 구조를 합성하고 캐시를 바인딩합니다. 예제 컨트롤러가 키 기반 행 편집과 저장 키 갱신을 추가합니다. 숨김 seq 필드 없이 현재 런타임과 같은 13자리 데이터와 PHP 제출을 사용합니다.',
    correctedNote: '원본 소스에서 명시적인 빈 컬렉션과 추가 버튼을 별도로 수정했습니다. 예제는 원본 공개 함수로 캐시 바인딩과 키 편집을 처리합니다. 수정 전 원본도 선택할 수 있습니다.',
    invalid: '검증에 실패했습니다. 필드 오류를 확인하십시오.',
    keyedNote: '템플릿을 한 번 컴파일합니다. 라이브러리 세션이 입력, 데이터 주입, 행 연산을 처리합니다. 네이티브 폼 이름에 행 식별자를 포함합니다. JSON 처리는 객체 멤버 순서를 유지해야 합니다.',
    load: '저장 데이터 다시 불러오기', blank: '빈 폼 마운트', save: 'JSON에 저장', validate: 'PHP 검증', reset: '저장 예제 초기화',
    saveNote: '저장은 이 프레임의 전체 JSON 저장소를 교체합니다. 초기화는 최초 레코드를 복원합니다. 검사는 각 항목 실행 전에 이 저장소를 초기화합니다.',
    names: '네이티브 input 이름과 값', state: '현재 폼 데이터', noState: '라이브러리 세션이 없으며 예제 컨트롤러가 값을 읽습니다.',
    php: 'PHP 요청, 검증, 저장 데이터, 응답', checks: '동일 검사 실행', allChecks: '모든 프레임워크 검사',
    results: '검증 결과', pending: '미실행', pass: 'PASS', fail: 'FAIL', running: '검사 중…',
    failureDetails: '검사 오류 원문',
    failureNotes: { original: {
      identity: '예제 구성: 배열 예제가 숨김 seq 필드를 추가합니다. 원본 렌더러가 요구하는 필드는 아닙니다.',
      exact: '원본 렌더러: 빈 부서 컬렉션에 빈 행을 생성하며 네이티브 제출 시 부서로 저장됩니다. empty 검사와 같은 원인입니다.',
      empty: '원본 렌더러: 명시적으로 빈 컬렉션을 전달해도 행 하나를 출력합니다. exact 검사와 같은 원인입니다.',
      cache: '예제 바인딩: 이 배열 예제는 갱신마다 buildForm을 호출합니다. 원본 공개 함수의 별도 합성 캐시를 검사하지 않습니다.',
    }, 'original-keyed': {
      exact: '원본 렌더러: 빈 부서 컬렉션에 숫자 인덱스의 임시 행을 생성하여 네이티브 제출이 키를 거부합니다. empty 검사는 같은 출력 동작을 확인합니다.',
      empty: '원본 렌더러: 명시적으로 빈 컬렉션을 전달해도 행 하나를 출력합니다. 예제 컨트롤러는 해당 행을 제거하지 않습니다.',
    } },
    render: '중첩 이름과 값', plus: '중첩 필드가 있는 회사 추가', copy: '현재 값 복사와 하위 행 독립 편집',
    identity: '숨김 seq 필드 없이 식별자 제출',
    keyedNames: '키 기반 데이터 렌더링과 숨김 필드 없는 네이티브 제출',
    nonsequential: '5 → 7 → 1: 삽입·복사·정렬·저장',
    transport: '지원하는 전송 형식의 순서 유지',
    order: '복사한 회사 정렬', inject: '편집 후 저장 데이터 재로드', saved: '복사 후 신규 ID 저장과 부모 관계 재로드',
    phpValid: '네이티브 폼의 PHP 검증', phpInvalid: '검증 실패 시 저장 레코드 유지', cache: '직렬화한 데이터 독립 템플릿 재사용',
    exact: '네이티브 제출과 재로드 후 전체 레코드 유지', empty: '빈 컬렉션 표시·추가·삭제·저장',
    deletion: '변경한 순서 저장과 하위 행 삭제', shape: '잘못된 컬렉션 자료형과 하위 ID의 부모 변경 거부',
    manual: '수정한 원본 렌더러와 현재 런타임에 동일한 키 데이터를 제공해 비교합니다. 수정 전 원본 키 렌더러도 선택할 수 있습니다. 행 버튼을 사용하거나 5 → 7 → 1 예제를 불러옵니다. 이전 숨김 필드 배열 진단도 선택할 수 있으며 승인된 대체 구현은 아닙니다.',
    source: '소스 커밋과 아카이브 해시', plusLabel: '+ 추가', copyLabel: '복사', minusLabel: '− 제거',
    upLabel: '↑', downLabel: '↓', count: '회사 행 수', download: '결과 다운로드', framework: '프레임워크',
  },
};
export function translations(language) { return messages[language === 'en' ? 'en' : 'ko']; }
