{
  "name": "crudui-cross-check-console",
  "version": "0.0.1",
  "private": true,
  "type": "module",
  "description": "Cross-check gateway: 4-language CRUDUI validation processes + HTML/React/Svelte/Vue rendering + static console",
  "main": "server.mjs",
  "scripts": {
    "build:go": "cd ../validators/go && go build -o validate .",
    "build:rust": "node build-rust.mjs",
    "build:validators": "node ../../../scripts/require-current-build.mjs && npm run build:go && npm run build:rust",
    "start": "node server.mjs",
    "dev": "node --watch server.mjs",
    "pretest": "npm run build:validators",
    "test": "node ../../../scripts/run-tests.mjs vitest --cwd examples/cross-check-console/server --root . --dir ."
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
